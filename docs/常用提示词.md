### 常用提示词

更新 gitnexus 索引，commit并push代码

- 阅读vitest相关技能，修复所有pnpm test报错及失败的测试用例，注意不要引入新的lint错误和类型错误。

- 修复pnpm test:e2e报错及失败的测试用例，注意不要引入新的lint错误和类型错误。

- 修复 CI=true pnpm lint 警告与错误，注意不要引入新的错误和类型错误。

- 修复 pnpm lint:rs 警告与错误，注意不要引入新的错误和类型错误。

- 修复pnpm knip警告与错误，注意不要引入新的lint错误和类型错误。

- 修复pnpm fmtpy警告与错误，注意不要引入新的错误和类型错误。

- 修复pnpm tsc警告与错误，注意不要引入新的lint错误和类型错误。

- 完整审查./src/，./src-tauri/下的代码，理解并梳理逻辑，然后重构项目代码目录结构和代码内容，使用SOLID原则，最大程度提高代码可读性和可维护性
- 由于最近`opencode go`等大模型供应商大幅提高了大模型价格，导致周限额也容易超出，目前需要在普通视图和气泡视图都添加周限额显示
- 需要新增一个`commandcode`的订阅监控。根据如下数据或者自行访问探索，如何添加`commandcode`的订阅，并使得其他人使用时需要配置参数最简单。我的监控面板地址是https://commandcode.ai/wenqieqiu/settings/usage
- 记到永久记忆和项目偏好文档里：全局代码探索优先使用`gitnexus mcp`或`codegraph mcp`，而不是直接读取
- 修复 README.md 里所有待解决问题
- 审查并更新README.md，可以使用 `gitnexus mcp`、`codegraph mcp`帮助探索代码
- 有问题，现在气泡模式下，tooltips窗口不出了，且偶尔出来也没有内容
- 研究并修复问题：从标准模式切换到气泡模式时，气泡组件右侧有部分会展示在屏幕外
- 除气泡模式的气泡外，所有窗口在每次打开时自适应内容宽高，且不出现滚动条。
使用omp的vision能力进行E2E验证

清理omp里过时的自定义技能，并去github升级所有第三方技能
整理当前会话遇到的问题，学习经验避免后续再踩坑
记到永久记忆：有最优决策则自动决定，除非是需要用户决定的才提问
记到全局记忆里：只要和代码相关就一定要优先使用gitnexus mcp或codegraph mcp帮助排查。这个很重要，直接关系到问题解决速度和质量
记到永久记忆以及AGENTS.md里：提问需要使用ask工具，且提供默认选项
完整审查当前项目。使用 gitnexus mcp、codegraph mcp探索代码
我期望新增一个脚本能一键测试free模型，使用`omp models find free`获取所有模型名称，测试脚本复用bench-models.ts
为当前项目添加`git hook`，以实现在代码commit后自动更新版本号，全程自动运行不需要手动确认

/skill:wayfinder 用 wayfinder "Work through the map" 模式，每次 claim 一个 frontier ticket 研究→关闭→更新 map

/skill:caveman use caveman ultra

```bash
$env:Path = [System.Environment]::GetEnvironmentVariable("Path", "Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path", "User")
$env:HTTPS_PROXY="http://127.0.0.1:7890"
$env:HTTP_PROXY="http://127.0.0.1:7890"
omp

export HTTPS_PROXY=http://127.0.0.1:7890
export HTTP_PROXY=http://127.0.0.1:7890
omp
```
